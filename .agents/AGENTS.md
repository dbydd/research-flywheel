# 共享目标记录

双子的共享目标：主线写这里，支线开成 todo 并直接带外部索引。

## 主线

把双子跑通一轮：castor 出第一步任务书交给 pollux，pollux 做完交回，castor 验收并收束。

判据：`runs/` 下出现一轮完整记录（本跳做了什么、产物在哪、交给 peer 的任务书全文），两个方向各至少跑过一次，收束由任务书里的判据决定。

## 支线

- [ ] 跑通第一轮接力并留下现场记录 —— 内容在 `runs/` 下本轮的目录（`notes.md` 写明本跳做了什么、留下哪些文件路径、交给 peer 的任务书全文）

## 索引

|条目|内容位置|
|---|---|
|双子行为约定与记事纪律|`.onlyne/AGENTS.md`|
|第一发任务书|`payload/first.md`|

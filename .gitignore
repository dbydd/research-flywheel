# OS / editor
.DS_Store
*.lock

# pi local state
.pi/sessions/
.pi/hindsight/
.pi/schedule-prompts.json

# onlyne / swarm runtime state (regenerate with `onlyne-swarm init` + `workspace sync`)
.onlyne/
_onlyne_workspaces/
**/onlyne_in/

# knowledge products stay tracked; runtime scratch does not
!.gitkeep

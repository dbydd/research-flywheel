# 本工作区尚未启用（gemini 双子模板，开发中）

我是本工作区第一个 pi 会话。这里是双子模板的开发区，不是运行区。

- 流程与运维正本：`README.md`。
- 拓扑唯一真相：`.onlyne/spec.toml`；role 记事骨架与模型位：`.onlyne/templates/gemini/<role>/`。
- 开发期的目标记录在 `.agents/AGENTS.md`，落分支时提升为仓根 `AGENTS.md`。

落定前不启动 onlyne server/client，不跑领域实验，不动 `spec.toml` 的结构与模板目录结构。

设计重点未决：产物与记事的形态。当前只有一条约定——工作文件路径由任务书点名，不设统一目录与固定格式。

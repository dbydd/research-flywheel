# 共享目标记录

## 记叙

主线：把双子跑通一轮——castor 出第一步任务书交给 pollux，pollux 做完交回，castor 验收并按判据收束。

判据：两条边各至少跑过一次，每一跳留下产物并把路径写进自己的记事，收束由任务书里的判据决定。

## 条目

支线：

- [ ] 跑通第一轮接力，两跳各留下产物与记事条目 —— 产物路径写进各自的 ws 记事，并进 complete 的 text

材料：

|条目|内容位置|
|---|---|
|第一发任务书|`payload/first.md`|

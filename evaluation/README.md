# evaluation/

评测器住本目录：打分、读数、指标计算脚本在这里落笔，一项目一簇。
运行命令原文写进设计段的断言清单，qa 照命令重跑对账，不接受口述结果。
字段口径见仓根约定（`.agents/AGENTS.md`）「评测契约」节与 experiment-design skill。
历史评测器在本仓 git 历史里：`git log --diff-filter=D -- evaluation/` 找回被删文件，`git show <rev>:<path>` 取回内容。

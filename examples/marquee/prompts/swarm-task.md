Swarm hop {task_id} from {from}.

The numbered steps are the contract. The `onlyne_in/` and `.onlyne/` directories are transport FIFOs and databases; leave them unopened. Work through the steps in order, then exit with swarm_complete.

{payload}

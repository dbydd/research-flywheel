# v1.0.0 重构轮纪要（2026-09-11）

主题：onlyne v1.0.0 破坏性适配（模板树 + ARIS 树），设计理念保持。三方：模板 supervisor（本会话）、omp（v1 主会话 01a08a49）、ARIS supervisor（01a08934）。

## 版本与工具链终态

- 工具链钉 **v1.0.0-beta.4 = 2666ede**（beta.2 因 plugins glob 回归作废；beta.3 兼容哨兵方案）。渠道：源码构建，crates.io/npm 无 v1。
- 装役完成：五产物入 ~/.cargo/bin，`onlyne version` = protocol:1。装机坑：cp 后 macOS exec 收 SIGKILL，须 `codesign --force --sign -`（入 BOOTSTRAP §2，omp 转 launch-pack 标准步骤）。

## 架构落点（模板树）

- 唯一真相 `.onlyne/spec.toml`：6 `[[client]]`（_supervisor 注册制 admin=true + 五 role），prose=身份，ACL=环，`[client.timeout/intent]` per-role。
- 内容模板 `.onlyne/templates/flywheel/<role>/`（AGENTS.md 深规 + settings 模型位 + `_supervisor` stub 过 generate 原子性）。
- 退役：`.schedule/`、root swarm.workspace.jsonc、onlyne_in、marquee 样例、v0 skills（换入 onlyne-supervisor/role 手册）。
- supervisor 走 admin 面（瘦入口 `onlyne --server-root .`），零上行边，completion 走 origin 豁免（omp 真机实证 queued 送达）。
- 后端：`ONLYNE_BACKEND=auto|zellij|orca|exec|fake`，探测序 zellij→orca→fake。端口默认 7812，查重条款（7813=ARIS live）。

## 关键裁决

1. **F4 vendor 形态**：`{{agent_package}}` 在 settings 渲成无 `../` 形态＝pi 0.85.1 拒载（ARIS beta.3 真机 + generate.rs 源码双证，推翻 omp 初版修法）；模板定稿**哨兵 `__AGENT_PACKAGE_ABS__` + 装配 sed 同步**（spec 字面值==settings 字面值触发 replace_bytes）；2666ede 起三态全合法，哨兵跨版本保持。真二进制实证 `../.onlyne/agent/onlyne-agent-pi` + vendor 有货。
2. **F1 死锁链**：非法 key 全量 parse 连 client init 不可跑；合法占位 key = `base64(32×0x01)=AQEBAQ…AQE=`（omp 手抄 AWAAA… 版作废）。
3. **全集环回归**：ARIS 评审揪出模板收简断边（critic→model 理论修订、writer→scout 补检索），补齐四条 ACL + prose 环语义，机器核双向闭合。

## 验收与提交链

- promote 九检夹具 9/9（含新门：packages 哨兵/abs、sed 同步交叉核、spec==templates、零 _supervisor 上行边）。
- omp 终验 18 项 F1 链 + e2e 全绿放行；三方无补记遗留。
- 提交链（origin/v3-swarm）：d18d731 重构主体 → 89902c9 全集环 → 578a568 验收三修 → b4bab8f 哨兵裁决 → …勘误 → 53ddb7f 端口条款 → 8ffe8e4 codesign 坑。
- 在途：ARIS 端到端迁移回执（其 7813 live=beta.3 绝对路径，升级随 restart 对齐）。
